import { Dispatch } from 'redux'

import {} from '@services'

import { <%= data.updateAction %> } from './constants'

import {} from '@types'

export interface UpdateHomeStoreActionType {
  type: <%= data.updateAction %>
}

export type <%= data.actionType %> = UpdateHomeStoreActionType
