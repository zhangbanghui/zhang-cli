import { <%= data.updateAction %> } from './constants'

import { <%= data.actionType %> } from './actionCreators'

const initState = {}

const reducer = (state = initState, action: <%= data.actionType %>) => {
  switch (action.type) {
    default:
      return state
  }
}

export default reducer
